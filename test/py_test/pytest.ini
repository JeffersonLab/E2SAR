[pytest]

# Test with modules: pytest -m b2b; pytest -m "unit or b2b"
markers =
    unit: Unit tests
    b2b: Back-to-back Segmentation & Reassembly tests without LB
    mockLB: Integration tests with a mock LB
    integration: Tests with a real EJFAT FPGA LB
